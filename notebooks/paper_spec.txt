# 论文数据一键生成脚本

## 项目路径
D:\Code\logistic-ai

## 任务
写一个 Python 入口脚本 notebooks/paper_data_generator.py，运行后自动生成论文需要的所有数据和图表。

## 依赖
- Python 3
- matplotlib（图表）
- numpy（数据处理）
- json（数据保存）
- 项目已有模块：src.rag (CaseRetriever, ExperienceModel, ExperienceModelV2)

## 功能清单

### 1. Case 统计分析
统计 1000 条 case 的分布：
- 事件类型 × 场景 热力图数据（20种事件 × 4种场景）
- 事件类型 × Action 分布
- Outcome 分布（success/failure）
- 保存到 notebooks/paper/case_distribution.json

### 2. 检索实验对比
三种方案对比：
- Baseline（五维打分 rerank）
- V1 经验模型（action 预测 + 一致性奖励）
- V2 经验模型（query-case 相关性权重学习）

指标：Action准确率、NDCG@5、决策时间（毫秒）
保存到 notebooks/paper/retrieval_comparison.json

### 3. 可视化图表（matplotlib，300 DPI，学术风格）

必须生成3张图：
1. fig_case_distribution.png
   - 子图1a: 事件类型×场景 热力图
   - 子图1b: Action分布柱状图  
   - 子图1c: Outcome饼图

2. fig_retrieval_comparison.png
   - 三种方案 Action准确率 对比柱状图
   - NDCG@5 对比柱状图
   - 决策时间 对比柱状图（对数坐标）

3. fig_retrieval_stability.png
   - 重复查询结果一致率

### 4. 数据汇总
所有数据保存到 notebooks/paper/paper_data.json

### 输出目录结构
notebooks/paper/
├── paper_data.json          # 完整数据汇总
├── case_distribution.json   # Case统计分析
├── retrieval_comparison.json # 检索实验结果
├── fig_case_distribution.png
├── fig_retrieval_comparison.png
└── fig_retrieval_stability.png

## 技术要求
- 图表风格：学术风格，中文标签，300 DPI
- 使用 matplotlib + numpy
- ExperienceModelV2 已存在于 src.rag.experience_model，直接 import 使用
- 所有数据保存为 JSON，方便 LaTeX 引用
- 入口文件：notebooks/paper_data_generator.py（运行即生成所有内容）

## ground_truth_action 函数（必须使用）
用于判断 Action 预测是否正确：
```python
def ground_truth_action(event_type, severity):
    if event_type in ("vehicle_breakdown", "vehicle_maintenance", "driver_unavailable"):
        if severity >= 5: return "reassign_order"
        elif severity >= 3: return "adjust_capacity"
        else: return "reroute"
    if event_type == "fuel_shortage":
        return "adjust_capacity" if severity >= 4 else "reroute"
    if event_type in ("traffic_accident", "traffic_congestion", "road_narrowing"):
        return "reroute" if severity >= 4 else "ignore"
    if event_type in ("road_closed", "bridge_weight_limit"):
        return "reroute" if severity >= 3 else "delay_tolerant"
    if event_type == "order_cancel":
        if severity >= 4: return "reroute"
        elif severity >= 2: return "delay_tolerant"
        else: return "ignore"
    if event_type == "order_modify":
        return "reroute" if severity >= 4 else "delay_tolerant"
    if event_type == "order_update": return "ignore"
    if event_type in ("priority_order_urgent", "delivery_failure"): return "reassign_order"
    if event_type == "demand_surge": return "adjust_capacity" if severity >= 4 else "delay_tolerant"
    if event_type == "demand_drop": return "delay_tolerant"
    if event_type in ("weather_delay", "natural_disaster", "public_event"):
        return "delay_tolerant" if severity >= 4 else "reroute"
    if event_type in ("warehouse_delay", "inventory_stockout"): return "delay_tolerant"
    return "reroute"
```

## coarse_score 函数（用于 Baseline）
```python
EVENT_TYPE_HIERARCHY = {
    "traffic_congestion": ["traffic_accident", "road_closed"],
    "weather_delay": ["traffic_accident"],
    "order_modify": ["order_cancel"],
    "order_update": ["order_cancel"],
    "demand_surge": [],
    "demand_drop": [],
}

def coarse_score(case, ev, sev, sc):
    score = 0.0
    if case.event_type == ev:
        score += 3.0
    else:
        fb = EVENT_TYPE_HIERARCHY.get(ev, [])
        if case.event_type in fb:
            score += 2.0
    score += max(0, 2.0 - abs(case.severity - sev) * 0.5)
    if case.scenario == sc:
        score += 1.0
    return score
```

## NDCG 计算
```python
def dcg_at_k(relevance, k=5):
    import math
    return sum(rel / max(1, math.log2(i + 2)) for i, rel in enumerate(relevance[:k]))

def ndcg_at_k(sorted_cases, gt_action, k=5):
    relevance = [1 if c.action == gt_action else 0 for c in sorted_cases[:k]]
    ideal = sorted(relevance, reverse=True)
    dcg = dcg_at_k(relevance, k)
    idcg = dcg_at_k(ideal, k)
    return dcg / idcg if idcg > 0 else 0.0
```

## 执行方式
完成代码后，运行：
```
cd D:\Code\logistic-ai
$env:DOUBAN_API_KEY='5f0d1c05-af99-4fb7-939d-e6529a31b04e' & python notebooks/paper_data_generator.py
```
